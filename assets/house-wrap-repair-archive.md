# Rear-wrap repair experiment preservation

All **17 scoped assets, 36,809,783 bytes**, have verified remote identities in `miguelemosreverte/mr-pinpin-archive`. The initial nine experiment assets passed remote download/SHA-256 verification. The historical extension resumed those nine proofs against unchanged remote identities, freshly verified five existing objects, and uploaded/read back three new objects: F’s illustrated base, F’s rear repair, and the expanded-room reference.

`house-wrap-repair.json` was generated by the existing `tools/assets/catalog.cjs` against a regular-file stage on the mini SSD. `house-wrap-repair-receipt.json` is the successful combined non-dry-run push receipt, with all identities checked against the manifest and both `verified` and `remote_verified` true for every entry. Separate unauthenticated HTTP GET and SHA-256/size checks cover all 17 assets: the original nine proofs are retained, and all eight historical additions were checked freshly. The combined anonymous evidence is retained externally at:

`/Volumes/TB4/mac-mini-storage/shared/pinpin-house-wrap-repair-20260924/anonymous-history-verification.json`

Preserved media:

- The unchanged native 1774×887 panorama used as the composite base.
- The exact 1400×1400 perspective extraction supplied to imagegen.
- The untouched 1254×1254 generated repair patch.
- The final 1774×887 bounded composite.
- Its analytic alpha preview and the identical pre-generation mask preview, under their reproducible filenames.
- The cyan mask-support illustration, explicitly diagnostic rather than artwork.
- The deterministic 1536×1024 cube PNG and its lossless WebP derivative.
- Historical A: `room-panorama-v3.webp` and all four `room-cube-{front,rear,up,down}-v1.webp` repairs.
- Historical F: `room-blender-illustrated-v2.webp` and `room-blender-rear-repair-v1.webp`.
- The historical comparison’s `room-expanded-v2.webp` reference. These eight are archive-role rows in this scoped manifest only; global production roles are unchanged.

The regular stage, upload log and original receipt remain under `pinpin-house-wrap-repair-20260924/archive-stage/`, `archive-upload.log` and `archive-receipt.json` on TB4. The eight-file extension is recorded in `archive-history-upload.log` and `archive-history-receipt.json`; its catalog contains exactly 17 distinct paths. Original anonymous proofs remain in `anonymous-verification.json`, and the extension script/evidence are `verify-history-anonymous.py` and `anonymous-history-verification.json`. Source working media may be specific symlinks to retained TB4 files; upload used regular files on the mini, not SMB atomic placement. No original media was deleted.

Restore into an external regular directory:

```sh
python3 tools/assets/hf_store.py pull \
  --manifest assets/house-wrap-repair.json \
  --root /Volumes/TB4/mac-mini-storage/shared/pinpin-house-wrap-repair-restored \
  --cache /Volumes/TB4/mac-mini-storage/shared/pinpin-asset-cache \
  --profile archive
```

Extraction, inverse projection, cube projection and archive transfers ran on the mini. A single WebP encode used existing Air Pillow with all I/O on TB4 because mini ffmpeg lacked its WebP encoder; decoded RGB equality was checked. Source scripts, records and `GEOMETRY.md` retain the exact projection/alpha recipe.

Preservation and projection checks do not approve artwork. Pixels outside the bounded support, including the pole caps, remain identical to the original, so its pre-existing pole and unrelated semantic defects remain. No global asset policy/catalog, previous tour selection, Git index or public release was changed by this scoped archive task.
