// Isolated read-only review mirror; byteRange adapted from scripts/atlas-traversal-trial/server.cjs.
const fs=require('node:fs'),fsp=fs.promises,path=require('node:path'),http=require('node:http');
const root=path.resolve(process.argv[2]),port=Number(process.argv[3]||8792);
const mime={'.html':'text/html','.js':'text/javascript','.css':'text/css','.json':'application/json','.md':'text/plain','.txt':'text/plain','.png':'image/png','.webp':'image/webp','.jpg':'image/jpeg','.mp4':'video/mp4'};
function byteRange(header,size){if(!header)return{start:0,end:size-1,partial:false};const m=/^bytes=(\d*)-(\d*)$/.exec(header);if(!m||(!m[1]&&!m[2]))throw{status:416};const start=m[1]?Number(m[1]):Math.max(0,size-Number(m[2]));const end=m[1]?(m[2]?Math.min(Number(m[2]),size-1):size-1):size-1;if(!Number.isSafeInteger(start)||!Number.isSafeInteger(end)||start>end||start>=size)throw{status:416};return{start,end,partial:true};}
http.createServer(async(req,res)=>{let size;try{if(!['GET','HEAD'].includes(req.method))throw{status:405};const name=decodeURIComponent(req.url.split('?')[0]).replace(/^\//,'')||'story-worlds.html';if(name.includes('\\')||name.includes('\0')||name.split('/').some(x=>!x||x.startsWith('.'))||!mime[path.extname(name)])throw{status:403};const file=path.join(root,name),stat=await fsp.stat(file);if(!stat.isFile())throw{status:404};size=stat.size;const r=byteRange(req.headers.range,size);res.setHeader('Content-Type',mime[path.extname(name)]);res.setHeader('Cache-Control','no-cache');res.setHeader('Accept-Ranges','bytes');res.setHeader('Content-Length',Math.max(0,r.end-r.start+1));res.setHeader('X-Content-Type-Options','nosniff');if(r.partial)res.setHeader('Content-Range',`bytes ${r.start}-${r.end}/${size}`);res.writeHead(r.partial?206:200);if(req.method==='HEAD'||!size)return res.end();fs.createReadStream(file,{start:r.start,end:r.end}).on('error',()=>res.destroy()).pipe(res);}catch(e){if(res.headersSent)return res.destroy();if(e.status===416&&size!==undefined)res.setHeader('Content-Range',`bytes */${size}`);res.writeHead(e.status||404);res.end('Review asset unavailable');}}).listen(port,'127.0.0.1',()=>console.log(JSON.stringify({root,port,pid:process.pid})));
