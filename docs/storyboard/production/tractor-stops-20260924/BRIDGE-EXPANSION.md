# Approved expansion: 24 viewpoints with reversible bridges

## Current decision after the moving-tractor feedback

### User acceptance of source matching and priority for completion

The user subsequently confirmed seeing one-to-one matching between the video frame and cubemap and accepted it. They explicitly deferred refinement of the original video's motion blur until **all cubemaps are achieved**. Preserve the exact source frame and its blur; do not delay the 24-viewpoint expansion to remove blur or polish focus transitions. Actual geometric cuts, duplicate subject fragments, rear wrap seams and pole defects still need review. This acceptance does not revive the rejected generated bridges or authorize official publication.

The historical plan below is superseded for transitions. The user rejected visible tractor movement during the first generated bridge. One revised locked-camera prompt was tested, but it deformed the vehicle more: outer wheel-center span overshoot increased from 27.1 to 38.5 pixels. Both stop-04 clips are retained as rejected attempts. A stop-13 request completed before the hold; it is not activated. Further paid bridge generation is stopped.

The active alternative projects the exact browser-decoded orbit frame into the spherical background, with identical opening camera and source camera. The optional `tractor-tour.html?sourceLock=1` review uses the same WebGL color path for orbit and panorama. At the exact 4-second anchor, the protected inner 88% has zero measured RGB difference. This preserves visible tractor geometry without a generated morph. It does not guarantee continuity in the surrounding forest: the first wider perimeter repair still has visible field-boundary joins after reapplying the original frame and is not selected.

Remaining panorama artwork continues in three bounded lanes: 00–05 excluding 04 plus 10–12; 06–09 plus 16–19; and 13–15 plus 20–23. New guide-first panoramas preserve the source framing much better than earlier attempts. Review the fully source-anchored result, including scenery borders and poles, before declaring a stop complete. Export cubes from that final stack. No source-only extraction or unreviewed generated image counts as a ready viewpoint.

See `STATIC-INTEGRATION.md`, `STATIC-ANCHOR-REVIEW.md`, `STATIC-TRACTOR-REVIEW.md`, and the reusable [source-frame workflow](../../panorama-workflow/source-frame-lock.md).

## Historical approved bridge plan

On 2026-09-24 the user accepted the calibrated V4 preview as much closer to the video and requested a bridge video from each orbit frame to its panorama opening, reversed on return, plus the other 23 perspectives. This supersedes the earlier pilot hold. It does not publish the official reader.

Each selected viewpoint retains its exact source frame, generated panorama and repairs, calibrated camera, final assembled sphere, cube export, and 1280×720 forward extraction. The forward extraction is the bridge end image. A single generated bridge serves both directions; the reverse is a local derivative of that clip. The viewing controls recenter to the canonical panorama camera before returning through the bridge.

The existing orbit remains the travel path. Its 24 recorded stops are distributed visual perspectives, not measured 15-degree camera positions. A generated bridge makes the difference between the two images gradual; it does not prove reconstructed geometry or pixel identity.

Production uses three independent lanes: artwork 00–12 excluding the already selected 04, artwork 13–23, and bridge generation/runtime controls. Media and computational work stay on the mini's TB4 disk. Per-stop `selected.json` records become available only after actual imagery, camera framing, and surroundings have been inspected. Each bridge pins both endpoint hashes. Missing or failed media must remain unavailable, never represented by placeholder readiness.

The image workflow uses the built-in imagegen tool. The mathematical guide is an edit target with a 65-degree authored vertical lens, not a measured video lens. A suitable final camera may differ when whole-machine inspection supports it. Per-stop reports distinguish raw feature fits from the selected camera. Targeted registered repairs correct demonstrated seam or pole defects. Prompts and returned masters remain immutable; later attempts are separate versions.

Video generation uses the existing PixVerse V6 transition runner. The current shortest supported one-second, silent 720p request is estimated at $0.045 per stop, or $1.08 for 24 first attempts; this is not an invoice. Reverse derivatives do not require more generation. Failed requests or deficient outputs are reviewed before any paid retry.
