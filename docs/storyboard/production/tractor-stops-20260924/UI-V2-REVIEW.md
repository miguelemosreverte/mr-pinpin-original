# V2 style comparison UI

`tractor-style-review.html` keeps the untouched stop04 video frame visible beside V1 and V2 panorama views. Each forward camera is fitted against that same raw source frame, using `pilot/style-v2/v1-fit-raw` and `v2-fit-raw`. No original-frame compositing, restoration substitution or image edits occur in the viewer.

The default comparison is stationary. Enable360° look-around to drag either panorama; their relative angular offsets and proportional zoom synchronize around their individually fitted views. Reset restores each fit; rear/up/down presets expose the spherical surround. Exact prompts, generation records and full panorama/fit links are available per trial. Pending V2 files are labeled rather than presented as completed.

Source syntax passed. A bounded direct-mini Chrome launch succeeded with no horizontal overflow; that early smoke did not await texture readiness. During review, the required empty-repair renderer defaults were corrected. Final two-texture desktop/mobile verification remains pending V2 output and fit. `verify-style-review.cjs` retains the focused actual-drag, prompt, image and responsive checks; final results will be appended after execution.

Final-check blocker: the Air later rejected process creation with EAGAIN / “Resource temporarily unavailable”, including a single `/bin/sh` with login disabled executing SSH. V2 is preserved on the mini, but its raw-reference fit and final browser run could not be launched from this lane. This is not a renderer pass or failure. No broad process termination was attempted.


## Resumed verification and selected demo

After the machine restart, both comparison panoramas loaded and actual desktop dragging synchronized their relative views. The subsequent exact-prompt check timed out because V2 prompt metadata had not yet reached the served mini pack; the coordinator copied those files afterward. No complete desktop/mobile comparison-suite pass is claimed.

The user then explicitly selected the original orbit-and-look interaction instead of the comparison page. `tractor-tour.config.js` now points stop04 at V2, its raw-frame fit, exact prompt and generation record. HTML/app/config imports carry `tour-render-v2`; previous accepted demos remain unchanged. The visible label reads “V2 lighting trial — framing mismatch”, and the V1/V2 comparison remains linked.

Focused actual-browser verification passed: V2 texture decoded/rendered; initial yaw0.008315083783520577, pitch0.007868339068120121 and vertical FOV89.4156598312888; a real drag crossed orbit97% through the endpoint to5%; an entering wheel gesture snapped to stop04 while retaining canonical yaw/pitch; a new wheel event after the quiet period rotated the panorama. No page errors occurred. The settled screenshot waited until canvas opacity1.

Evidence: `TOUR-V2-RESULTS.json`, reproducible `verify-tour-v2.cjs`, and external mini `browser-check/tour-v2-look.png`. This verifies behavior, not appearance approval: the fit centers the tractor but peripheral wheels/trailer stretch or crop. No exact source texture is composited. The coordinator opened the updated orbit-mode demo for the user.
