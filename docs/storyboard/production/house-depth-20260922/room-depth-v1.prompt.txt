Use case: precise-object-edit. Asset type: pixel-aligned depth pass for the supplied illustrated room, used by an interactive 2.5D camera.
Input image 1 is the exact edit target / geometry reference, NOT a style inspiration.
Create ONLY a smooth grayscale depth map of this exact image at 1536x1024. WHITE means nearest to the viewer, BLACK means most distant. This is geometric camera depth, not brightness, shading, a pencil drawing, or an edge map.
Preserve the exact crop, camera, silhouettes, all room architecture, and every object position pixel-aligned with the original. Do not redraw/recompose the room. No text, border, labels, gradients unrelated to actual depth, lighting, cast shadows, woodgrain, rug fibers, leaf texture, or colored pixels.
Geometry: nearest foreground at bottom left (cushioned seat/flowers) and bottom right wooden furniture/flowers should be very pale gray. The floor is a continuous perspective plane, pale near the bottom edge and gradually darker toward the back wall; the round braided rug lies on that same plane and must NOT get ring-pattern texture in depth. The back wall around the circular window is middle/darker gray and continuous. The large bookcase on the right has coherent shelf/front-edge/back-panel depth with clear silhouettes, small modest depth separation for books and pots, no painted cover detail. The open green door leaf on the left is a nearer flat slab with a depth gradient matching its angle, not a dark hole. Door frame, window frame and spokes preserve their exact outlines. Distant forest seen THROUGH the open doorway and THROUGH the round window must be nearly black, regardless of the original sunlight and color. Distinguish these outdoor recesses sharply from the nearer door leaf/frames. Plants, stool, basket, jars and shelves have plausible smooth volumes. Small knob/hinges need only simple depth form.
Return one opaque grayscale depth image ONLY, with gently smooth depth within surfaces and accurate abrupt occlusion boundaries. No color illustration. Keep all original geometry.
